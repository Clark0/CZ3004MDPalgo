package mdpalgo.models;

import mdpalgo.constants.Direction;

// TODO
public class RealRobot extends Robot {
    public RealRobot(int row, int col, Direction direction) {
        super(row, col, direction);
    }
}
